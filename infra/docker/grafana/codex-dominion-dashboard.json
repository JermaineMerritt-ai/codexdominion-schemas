{
  "id": null,
  "title": "Codex Dominion Constellation",
  "tags": ["codex", "dominion", "argo-rollouts", "prometheus"],
  "timezone": "browser",
  "panels": [
    {
      "type": "gauge",
      "title": "HTTP Success Rate",
      "datasource": "Prometheus",
      "targets": [
        {
          "expr": "sum(rate(http_requests_total{code!~\"5..\"}[1m])) / sum(rate(http_requests_total[1m]))"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "thresholds": {
            "mode": "absolute",
            "steps": [
              { "color": "red", "value": 0 },
              { "color": "yellow", "value": 0.95 },
              { "color": "green", "value": 0.99 }
            ]
          },
          "unit": "percent"
        }
      }
    },
    {
      "type": "timeseries",
      "title": "95th Percentile Latency",
      "datasource": "Prometheus",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket[5m])) by (le))"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "s"
        }
      }
    },
    {
      "type": "barchart",
      "title": "Rollback Events (per hour)",
      "datasource": "Prometheus",
      "targets": [
        {
          "expr": "increase(argo_rollouts_rollback_count[1h])"
        }
      ]
    },
    {
      "type": "timeseries",
      "title": "Canary Weight Progression",
      "datasource": "Prometheus",
      "targets": [
        {
          "expr": "argo_rollouts_canary_weight{rollout=\"orchestrator\"}"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "percent"
        }
      }
    }
  ],
  "schemaVersion": 36,
  "version": 1
}
