# Production Codex Dashboard - HTTPS
server {
    listen 443 ssl;
    server_name aistorelab.com www.aistorelab.com;

    ssl_certificate /etc/letsencrypt/live/aistorelab.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/aistorelab.com/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:8501/;  # trailing slash is critical for Streamlit
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Production Codex Dashboard - HTTP to HTTPS redirect
server {
    listen 80;
    server_name aistorelab.com www.aistorelab.com;

    return 301 https://aistorelab.com$request_uri;
}

# Staging Codex Dashboard - HTTPS
server {
    listen 443 ssl;
    server_name staging.aistorelab.com;

    ssl_certificate /etc/letsencrypt/live/staging.aistorelab.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/staging.aistorelab.com/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:8502/;  # trailing slash is critical for Streamlit
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Staging Codex Dashboard - HTTP to HTTPS redirect
server {
    listen 80;
    server_name staging.aistorelab.com;

    return 301 https://staging.aistorelab.com$request_uri;
}
