apiVersion: codexdominion/v1
kind: Portal
metadata:
  name: continuum-map-portal
  version: 1.0.0
spec:
  purpose: "Visual onboarding — narrates cycles (daily, seasonal, epochal)"
  binding:
    artifacts: [hymns/, seals/]
    dashboards: [continuum.json]
