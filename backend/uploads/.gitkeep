# Uploads Directory

This directory stores uploaded files from the CodexDominion system.

## Structure
- Files are stored with unique timestamped filenames
- Original filenames are preserved in metadata
- Access files via: `http://localhost:8080/uploads/filename`

## Security
- Max file size: 50MB
- File type validation per upload type
- Authentication required for all uploads
