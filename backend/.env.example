DATABASE_URL="postgresql://codex:codex@localhost:5432/codexdominion"
JWT_SECRET="super-secret-dev-key"
JWT_REFRESH_SECRET="another-secret-key"
JWT_EXPIRATION="15m"
JWT_REFRESH_EXPIRATION="7d"
PORT=4000
NODE_ENV="development"

# ==========================================================
# ACTION AI - FOLLOW-UP EXECUTOR V0 CONFIGURATION
# ==========================================================

# SMTP Configuration (for email sending via EmailSenderService)
# Leave blank to run in TEST MODE (emails logged but not sent)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER=""  # Your email address
SMTP_PASS=""  # Your email password or app-specific password
SMTP_FROM_EMAIL=""  # From email address (defaults to SMTP_USER)
SMTP_FROM_NAME="Action AI"  # From name in emails

# Detector Configuration (optional - has defaults)
# DETECTOR_INVOICE_SCHEDULE="0 * * * *"  # Cron: every hour (default)
# DETECTOR_LEAD_SCHEDULE="0 */6 * * *"   # Cron: every 6 hours (default)
# DETECTOR_STALE_THRESHOLD_DAYS="7"      # Lead stale after N days (default: 7)

